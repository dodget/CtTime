
<% if user_signed_in? && current_user.admin? %>
  <ul>
    <% @users.each do |user| %>
      <li class="list-group-item">
        <%= link_to user.full_name, user %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h1>Sorry! You are not logged in as an admin</h1>
<% end %>
